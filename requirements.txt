# ─────────────────────────────────────────────────────────────
#  Backend (FastAPI) dependencies — this is what run.sh installs
#  and what the React frontend talks to.
# ─────────────────────────────────────────────────────────────

# API framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20   # multipart form parsing for PDF statement uploads

# Database
sqlalchemy==2.0.36

# Settings / env
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Plaid SDK (live sync — dormant until Plaid dev access is granted)
plaid-python==40.1.0

# Fidelity OFX direct connect (dormant until credentials are provided)
ofxtools==0.9.5

# Encryption (Plaid tokens at rest)
cryptography==44.0.0

# Background scheduler (auto-sync)
apscheduler==3.10.4

# Document import — PDF bank-statement parsing (primary data path)
pdfplumber==0.11.4
pymupdf==1.23.26

# ─────────────────────────────────────────────────────────────
#  Legacy Streamlit frontend (frontend/) — SUPERSEDED by
#  react-frontend/ and NOT launched by run.sh. Removed from the
#  default install to keep Replit boots fast. To run the old
#  Streamlit UI, reinstall:
#      streamlit==1.41.1  plotly==5.24.1  pandas==2.2.3
#      httpx==0.28.1      jinja2==3.1.5
# ─────────────────────────────────────────────────────────────
